const Letter = require("./Letter")


const Word = function (letters){
    this.letters = letters,
    this.display = function displayWord(){
        if (i=0, i < this.letters, i++)
        console.log (letters)
    },
    this.guess = function assessment(usrGuess){

    } 

}

function contains(guess){
    if (i=0, i < Word.letters, i++){
        console.log(Word.letters)
    }
}

module.exports = contains;
module.exports = Word;